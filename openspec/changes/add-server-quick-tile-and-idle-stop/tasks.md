## 1. Настройка автоостановки

- [ ] 1.1 Добавить `IdleStopTimeout` (OFF, 15, 30, 60 минут; по умолчанию 30) в `DeviceSettings`, DataStore с чтением неизвестного значения как default, репозиторий и use case; проверить JVM-тестами DataStore и валидации.
- [ ] 1.2 Добавить в Settings карточку «Сервер» с выбором времени автоостановки; проверить Compose-тестом и тестом ViewModel.

## 2. Живые подключения и простой

- [ ] 2.1 Публиковать `activeConnectionCount` в `BrowserSessionCoordinator` из `attachConnection`/`detachConnection`; проверить JVM-тестом, что закрытие WebSocket уменьшает счётчик, а session остаётся.
- [ ] 2.2 Реализовать `IdleStopController`: условия простоя (нет подключений, pending pairing и незавершённых operations), дедлайн по `elapsedRealtime`, сброс при активности, остановка с `ServerStopReason.IdleTimeout`, перезапуск отсчёта при смене настройки; проверить JVM-тестами с виртуальным временем, включая «сон» (скачок часов) и длинную передачу.
- [ ] 2.3 Сохранять `IdleTimeout` как причину Stopped в состоянии и journal и показывать на Home «Сервер остановлен: N минут без подключений» без стиля ошибки; проверить тестом coordinator и Compose-тестом Home.
- [ ] 2.4 Показывать в уведомлении время остановки, пока идёт отсчёт, и убирать строку при подключении; проверить JVM-тестом `ServerNotificationModel`.

## 3. Плитка быстрых настроек

- [ ] 3.1 Добавить `DeviceBridgeTileService` с разрешением `BIND_QUICK_SETTINGS_TILE`, иконкой и подписями; отображать active/inactive/unavailable и подзаголовок «Запущен · N браузеров», «Остановлен» или «Запускается…» по `ServerLifecycleState`; проверить JVM-тестом маппинга состояния в модель плитки.
- [ ] 3.2 Реализовать нажатие на остановленный сервер: `unlockAndRun` на заблокированном телефоне, проверка разрешений, прямой `startForegroundService` и fallback `startActivityAndCollapse` с `START_FROM_TILE`, после которого `HomeViewModel` запускает сервер на видимом экране; проверить тестами HomeViewModel и policy.
- [ ] 3.3 Реализовать нажатие на работающий сервер: немедленная остановка без активных operations и подтверждение через `showDialog` при активной передаче; проверить тестом логики решения.
- [ ] 3.4 Обновлять плитку через `requestListeningState` при каждом изменении lifecycle и числа браузеров; проверить на эмуляторе, что плитка меняется при запуске с Home и при автоостановке.
- [ ] 3.5 В Settings (API 33+) добавить «Добавить плитку в быстрые настройки» через `requestAddTileService`, для API < 33 — текстовую инструкцию; проверить Compose-тестом.

## 4. Документация

- [ ] 4.1 Обновить `docs/user-guide.md` (плитка, автоостановка по умолчанию 30 минут, как выключить) и ТЗ; выполнить `openspec validate add-server-quick-tile-and-idle-stop --strict`.

## 5. Проверка

- [ ] 5.1 Прогнать `:app:testDebugUnitTest`, `:app:lintDebug`, `:app:verifyReleasePolicy`, web gates и `connectedDebugAndroidTest` на API 29 и API 37.1; проверить отсутствие регрессий.
- [ ] 5.2 E2E на release-сборке, API 29 и 37.1: добавить плитку, запустить и остановить сервер плиткой при свёрнутом приложении и зафиксировать, нужен ли fallback через Activity; проверить запрос разблокировки на заблокированном экране и подтверждение остановки во время передачи.
- [ ] 5.3 E2E автоостановки: на debug-сборке с тестовым интервалом 1 минута (доступен только в debug) проверить остановку без подключений, сброс отсчёта при подключении, отсутствие остановки во время передачи, сообщение на Home и неактивную плитку; на release-сборке один раз дождаться остановки через 15 минут без подключений.
