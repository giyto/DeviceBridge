## 1. Зависимости и тестовая основа

- [ ] 1.1 Выполнить исходные `gradlew.bat :app:testDebugUnitTest :app:lintDebug :app:assembleDebug` и зафиксировать, что шаблонный проект собирается до изменений.
- [ ] 1.2 Добавить совместимые зависимости Navigation Compose, Lifecycle ViewModel Compose и Compose UI tests через version catalog, затем проверить Gradle Sync и `gradlew.bat :app:assembleDebug`.

## 2. Корневое приложение и навигация

- [ ] 2.1 Сначала написать Compose UI-тесты стартового раздела, перехода в «Историю» и возврата на «Главную» и убедиться, что они падают до реализации app shell.
- [ ] 2.2 Реализовать в пакете `app` типобезопасные верхнеуровневые destinations и `DeviceBridgeApp` с `NavHost`, чтобы тесты навигации проходили.
- [ ] 2.3 Упростить `MainActivity` до установки темы и запуска `DeviceBridgeApp`, затем Compose UI-тестом проверить восстановление выбранного раздела после повторного создания Activity.

## 3. Состояние серверной сессии и главный экран

- [ ] 3.1 Сначала написать unit-тесты начального состояния `Stopped`, недоступности действий «Текст»/«Файлы» и отсутствия фиктивного адреса, кода и браузера; убедиться, что тесты падают без модели состояния.
- [ ] 3.2 Реализовать платформенно-независимый статус серверной сессии в `domain`, неизменяемый `ServerSessionUiState` и ViewModel в `feature/home`, затем добиться прохождения unit-тестов без Android/Compose импортов в domain.
- [ ] 3.3 Сначала написать Compose UI-тест статуса «Сервер остановлен», disabled-действий и пояснения «запустить сервер — подключить браузер» и убедиться, что он падает до реализации главного экрана.
- [ ] 3.4 Реализовать главный экран и реально переиспользуемые компоненты карточки состояния/быстрых действий в `core/ui`, затем добиться прохождения Compose UI-теста и проверить previews в светлой и тёмной теме.

## 4. История, настройки и доступность

- [ ] 4.1 Сначала написать Compose UI-тесты пустой истории, базового экрана настроек и accessibility labels навигации и убедиться, что они падают до реализации экранов.
- [ ] 4.2 Реализовать `feature/history` и `feature/settings` без вымышленных данных и не создавать пустой пакет `data`, затем добиться прохождения соответствующих UI-тестов.
- [ ] 4.3 Проверить светлую и тёмную темы, системный масштаб шрифта 200% и TalkBack semantics на AVD API 37.1 и зафиксировать результат в change notes или commit message.

## 5. Итоговая проверка change

- [ ] 5.1 Выполнить `gradlew.bat :app:testDebugUnitTest :app:lintDebug :app:assembleDebug` и исправить все ошибки, относящиеся к change.
- [ ] 5.2 Выполнить `gradlew.bat :app:connectedDebugAndroidTest` отдельно на AVD API 29 и API 37.1 и вручную проверить запуск, три раздела, статус «Сервер остановлен», пустую историю и недоступные действия передачи.
