## 1. Android: имя в сети (mDNS)

- [ ] 1.1 Реализовать `MdnsCodec`: разбор запросов (вопросы, бит QU, записи в секциях answer и authority) и сборку ответов A, NSEC «нет AAAA», проб и goodbye. Защиты: пакет до 9000 байт, указатели сжатия только назад и не больше 16 переходов, ограниченное число записей. Проверка: JVM-тесты на эталонных байтах (запрос Windows A+AAAA, legacy unicast, QU), на регистронезависимое сравнение имени и на отказ без исключения для обрезанных пакетов, петель сжатия и огромных счётчиков
- [ ] 1.2 Реализовать `LocalNameResponder` поверх транспорта-интерфейса и часов. Что он делает:
  - проверяет имя тремя пробами по 250 мс и перебирает суффиксы `-2`…`-9` при конфликте;
  - по правилу RFC 6762 §8.2 решает, кто уступает при одновременной пробе;
  - делает 2 объявления после проверки;
  - отвечает многоадресно не чаще раза в секунду на запись, и одноадресно на QU и legacy-запросы;
  - игнорирует запросы вне своей подсети;
  - при конфликте во время работы перестаёт отвечать;
  - при остановке отправляет goodbye.

  Проверка: JVM-тесты с поддельным транспортом и часами на каждое правило, включая «все имена заняты»
- [ ] 1.3 Реализовать Android-транспорт на `MulticastSocket`:
  - `SO_REUSEADDR` до bind на 5353, привязка к интерфейсу endpoint, вход в группу 224.0.0.251;
  - `MulticastLock` на время работы;
  - разрешение `CHANGE_WIFI_MULTICAST_STATE` в манифесте;
  - обновлённые `ServerManifestContractTest` и `ServerManifestInstrumentedTest`.

  Проверка: контрактные тесты манифеста и instrumented-тест на эмуляторе API 37: тестовый клиент на том же интерфейсе отправляет запрос A и получает адрес сервера, а после остановки получает goodbye
- [ ] 1.4 Встроить ответчик в `KtorServerRuntime`:
  - порядок запуска: IP → проверка имени → `prepareTls(ip, name)` → слушатели → объявление;
  - goodbye и освобождение lock при любой остановке;
  - если сокет не открылся, сервер работает по IP со статусом `Unavailable(NETWORK)`.

  Проверка: JVM-тесты runtime с поддельным ответчиком: имя занято, недоступно, конфликт во время работы, остановка из-за смены сети отзывает имя

## 2. Android: адрес и проверка Host

- [ ] 2.1 Расширить `ServerEndpoint`: `localName`, `nameStatus`, `ipUrl`; `url` по имени, если оно есть. `allowedAuthorities = {ip:port, name:port}` с обновлением при потере имени во время работы. Проверка: JVM-тесты модели и маршрутов:
  - `Host: devicebridge.local:8787` с соответствующим Origin принимается для страницы, API и WebSocket;
  - чужое имя `.local`, имя без порта и имя после потери получают 403;
  - в защищённом режиме перенаправление ведёт на `https://devicebridge.local:8787`
- [ ] 2.2 Главный экран:
  - главный адрес по имени и кнопка «Копировать адрес»;
  - строка «Если не открывается» с IP и своей кнопкой копирования;
  - объяснение, если имя занято другим устройством, недоступно в сети или не подходит к сертификату, со ссылкой в настройки.

  Уведомление сервера показывает адрес по имени. Проверка: Compose-тесты `HomeScreen` на каждый `nameStatus` и JVM-тест `ServerNotificationModelFactory`

## 3. Android: настройка «Имя в сети»

- [ ] 3.1 Добавить `NetworkName` в domain (правила из спеки), ключ `network_name` в `DataStoreSettingsRepository` с заменой неверного значения на `devicebridge`, use case и поле в `SettingsViewModel`. Проверка: JVM-тесты проверки (регистр, кириллица, пробел, дефис по краям, 40/41 символ) и чтения неверного значения
- [ ] 3.2 Поле «Имя в сети» с суффиксом `.local` в карточке «Устройство», понятная ошибка и подсказка «Новый адрес заработает после перезапуска сервера», пока сервер запущен. Проверка: Compose-тест `SettingsScreen` на сохранение, ошибку и подсказку

## 4. Android: сертификаты

- [ ] 4.1 Новые корни с permitted dNSName `local` в `X509Profiles.nameConstraints()`. Проверка: JVM-тест `CertPathValidator` с включённой проверкой name constraints:
  - сертификаты для `nikita.local` и `devicebridge-2.local` принимаются;
  - `example.com`, `local.example.com` и публичный IP отклоняются
- [ ] 4.2 Разборщик permitted dNSName из расширения NameConstraints корня и `LocalCertificateAuthority.permits(name)`. Сертификат сервера содержит IP и занятое имя (без безусловного `devicebridge.local`), а `covers(ip, name)` проверяет оба значения. Проверка: JVM-тесты:
  - старый корень допускает только `devicebridge.local`, новый допускает любое `.local`;
  - смена имени перевыпускает сертификат сервера, а тот же IP и имя его не перевыпускают
- [ ] 4.3 В защищённом режиме имя, которое корень не допускает, не занимается (`Unavailable(CERTIFICATE)`), а раздел «Защищённое соединение» показывает подсказку сбросить сертификат. Проверка: JVM-тест runtime со старым корнем и именем `nikita` и Compose-тест подсказки

## 5. Android: решение запроса из уведомления

- [ ] 5.1 Действия `DENY_PAIRING`, `ALLOW_PAIRING` и `ALLOW_AND_REMEMBER_PAIRING`:
  - `ACTION_SLOTS = 16` и `CONTENT_SLOT = 15`;
  - кнопки разрешения только на API 31+ с `setAuthenticationRequired(true)`, «Разрешить и запомнить» только при просьбе браузера;
  - на API 29-30 кнопка «Отклонить» и текст «Откройте DeviceBridge, чтобы разрешить».

  Проверка: JVM-тесты `EventNotificationModelFactory` для обоих уровней API и с просьбой запомнить и без неё, а также instrumented-тест публикации на API 37: у кнопок разрешения установлен признак разблокировки
- [ ] 5.2 Обработчик в `EventNotificationActionHandler` вызывает те же use case, что и `HomeViewModel`. Если запрос уже решён или истёк, ничего не меняет и снимает уведомление. Проверка: JVM-тесты обработчика на три действия и на «запрос уже решён», а также проверка, что «Разрешить» не запоминает браузер

## 6. Web: ожидание телефона

- [ ] 6.1 Реализовать `AvailabilityWaiter`:
  - опрос `/web-manifest.json` каждые 3 с при видимой вкладке и 15 с при скрытой;
  - немедленная проверка на `visibilitychange`, `online` и «Проверить сейчас»;
  - не больше одного запроса одновременно.

  Проверка: vitest с поддельными таймерами, видимостью и fetch
- [ ] 6.2 Состояние `waiting` в `SessionController`: оно заменяет конечные `needsUserAction(reconnect_exhausted)` и `offline`. При успехе ожидания запускается `beginNewCycle()` + `boot()`. Отзыв при работающем сервере (1008) в ожидание не ведёт. Проверка: vitest:
  - сервер вернулся → запомненный вход обменян без кода;
  - без запомненного входа → форма с пометкой «Телефон снова доступен»;
  - 1008 → `sessionLost`;
  - черновики сохранены, код и передачи не повторяются
- [ ] 6.3 Вид состояния `waiting` в `shellView` («Телефон недоступен», «Страница подключится сама, когда сервер запустится», кнопка «Проверить сейчас»). `eventNotificationController` считает `waiting` потерей связи. Проверка: vitest вида и контроллера уведомлений, visual-снимок состояния

## 7. Web: вход и подсказки

- [ ] 7.1 Галочка «Запомнить этот браузер» включена по умолчанию и после сброса формы. Первый challenge создаётся с `rememberBrowserRequested = true`. Проверка: обновлённые vitest `shellView` и `sessionController`, visual-тест, где Space выключает галочку, и обновлённые снимки
- [ ] 7.2 Сообщение «Этот телефон не узнал браузер» с тремя причинами после отказа обмена запомненного входа. Проверка: vitest `sessionController` и `shellView`, compat-тест отзыва ожидает сообщение
- [ ] 7.3 `browserInstallHintPreferenceStore` (ключ `devicebridge.install-hint.v1`) и подсказка про ярлык по условиям из design.md, с кнопкой закрытия `data-action`. Проверка: vitest хранилища и контроллера:
  - Chrome и Edge по имени показывают подсказку;
  - по IP, в Firefox, в установленном приложении и после закрытия подсказки нет;
  - при недоступном хранилище страница работает без ошибок;
  - `accessibilityStructure` и `privacy.contract` проходят
- [ ] 7.4 Фраза о подмене телефона в предупреждении HTTP. Проверка: обновлённый тест `shellView` на текст предупреждения и visual-снимки

## 8. Документация и проверка

- [ ] 8.1 `docs/user-guide.md`:
  - адрес по имени и запасной IP;
  - «Имя в сети» и два телефона;
  - один повторный вход при переходе с IP на имя;
  - сброс старого сертификата для своего имени;
  - ярлык на рабочем столе;
  - кнопки в уведомлении о запросе.

  Проверка: разделы присутствуют и совпадают с текстами интерфейса
- [ ] 8.2 Полный прогон: JVM-тесты, lint, web (vitest, compat, visual), instrumented на эмуляторе API 37 только через `am instrument` и E2E на эмуляторе:
  - вход по запомненному браузеру после перезапуска сервера без перезагрузки вкладки;
  - «Разрешить» из уведомления.

  Результаты записать в `docs/verification/local-name-and-quick-entry.md`
- [ ] 8.3 Ручная проверка на телефоне и компьютере пользователя:
  - Windows открывает `http://devicebridge.local:8787` при профиле сети «Частная» и «Общедоступная»;
  - HTTPS по имени после сброса сертификата;
  - второе устройство с тем же именем даёт `devicebridge-2`;
  - названия пунктов меню в подсказке про ярлык совпадают с Chrome и Edge.

  Итог записать в тот же файл проверки
