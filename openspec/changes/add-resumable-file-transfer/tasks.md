## 1. Протокол и хранилище незавершённых частей

- [ ] 1.1 Поднять `FILE_PROTOCOL_VERSION` до 2 в Kotlin и TS, добавить `resumeOffsetBytes` в событие перехода в TRANSFERRING и `resumableBytes` в snapshot item; проверить parser-тесты обеих сторон, включая отказ для версии 1.
- [ ] 1.2 Добавить Room-таблицу `partial_uploads` (sha256, size, name, treeUri, partialDocumentUri, mime, bytes, updatedAt) с миграцией 1 → 2 и экспортом схемы; проверить migration-тест и то, что правила backup по-прежнему исключают БД.
- [ ] 1.3 Реализовать репозиторий незавершённых частей: поиск по ключу, upsert, удаление, cleanup записей старше 24 ч и без существующего partial; проверить JVM-тестами с фиктивными часами и фиктивным provider.

## 2. Android: сохранение и продолжение upload

- [ ] 2.1 Разделить завершение partial на `abort()` и `retain()`: network failure, потеря session и остановка сервера для файлов ≥ 8 МиБ сохраняют partial и запись, а отмена, checksum mismatch и мелкие файлы удаляют; проверить JVM-тестами всех веток.
- [ ] 2.2 Открывать partial в режиме `"rw"` с `truncate`/`position(offset)` и fallback на запись с нуля, если provider не даёт seekable descriptor; проверить тестом с фиктивным provider без seek.
- [ ] 2.3 Восстанавливать SHA-256 по фактическому префиксу partial перед приёмом и использовать прочитанную длину как offset; проверить тестом с усечённым и повреждённым префиксом, где итоговое несовпадение даёт checksum mismatch и удаление части.
- [ ] 2.4 Сопоставлять подтверждённый offer с записью по SHA-256, размеру, имени и папке, включая автоприём; при другой папке начинать с нуля; проверить JVM-тестами coordinator.
- [ ] 2.5 Изменить retry: item с сохранённой частью возвращается в QUEUED с `bytes = resumableBytes`, destination lease не удаляется и повторный approve в том же generation не нужен; проверить тестами scheduler и coordinator.
- [ ] 2.6 Принимать `POST /api/v1/files/{id}` с заголовком `X-DeviceBridge-Upload-Offset`, отклонять несовпадающий offset кодом `409` без открытия output и проверять `Content-Length = size − offset`; проверить route-тестами.
- [ ] 2.7 Учитывать при проверке свободного места только оставшиеся байты; проверить тестом.
- [ ] 2.8 Запускать cleanup при старте приложения и сервера; проверить тестом, что просроченная часть удаляется и следующий offer начинается с нуля.

## 3. Android: продолжение download

- [ ] 3.1 Отдавать `Accept-Ranges: bytes` и `ETag: "sha256-<hex>"`, обрабатывать одиночный `Range` ответом `206` и `Content-Range`, `If-Range`/ETag mismatch ответом `412`, прочие диапазоны ответом `416`; проверить route-тестами.
- [ ] 3.2 Разрешить повторное использование grant для ranged-продолжения того же item в течение 15 минут после обрыва в том же generation, переводить item FAILED → TRANSFERRING с progress от offset и отвечать `409`, если направление занято; проверить тестами grant registry и coordinator.

## 4. Web

- [ ] 4.1 Отправлять `file.slice(offset)` с заголовком offset и корректным progress от offset; проверить Vitest-тестом uploader.
- [ ] 4.2 Показывать «Продолжить» с уже переданным объёмом для failed item с `resumableBytes`, этап «Проверка сохранённой части» и продолжение с N МБ для совпавшего offer; проверить Vitest-тестами view и controller.
- [ ] 4.3 Для прерванного download показывать подсказку о «Возобновить» в менеджере загрузок браузера и повтор с начала; проверить Vitest-тестом и обновить visual-снимки, если меняется карточка.

## 5. Android UI

- [ ] 5.1 На экране файлов и Home показывать «Продолжить» и «Продолжение с N МБ» вместо «Повторить», если сохранена часть; проверить Compose-тестами.
- [ ] 5.2 В Settings (карточка «Передача файлов») показать число и размер незавершённых файлов с действием «Удалить незавершённые»; проверить Compose-тестом и тестом ViewModel.

## 6. Документация

- [ ] 6.1 Обновить `docs/user-guide.md` (продолжение, 24 ч, удаление, возобновление download) и ТЗ; выполнить `openspec validate add-resumable-file-transfer --strict`.

## 7. Проверка

- [ ] 7.1 Прогнать `:app:testDebugUnitTest`, `:app:lintDebug`, `:app:verifyReleasePolicy`, полный набор web gates и `connectedDebugAndroidTest` на API 29 и API 37.1; проверить отсутствие регрессий.
- [ ] 7.2 E2E на release-сборке, API 29 и 37.1: оборвать upload 500 МиБ на середине отключением Wi-Fi эмулятора, перезапустить сервер, переподключиться, отправить тот же файл и убедиться, что передача продолжилась с сохранённого offset, а SHA-256 на телефоне совпал; повторить «Продолжить» в той же вкладке при обрыве WebSocket.
- [ ] 7.3 E2E download: оборвать ranged-загрузку 500 МиБ в Chrome, возобновить её штатно и сравнить SHA-256 скачанного файла; проверить отказ после 15 минут и после перезапуска сервера.
