## 1. Android: основа

- [x] 1.1 Реализовать `AppVisibilityTracker` на `ActivityLifecycleCallbacks` (счётчик Activity между `onResume` и `onPause`, `StateFlow<Boolean>`) и зарегистрировать его в `DeviceBridgeApplication`. Проверка: JVM-тест счётчика, включая перекрытие двух Activity и сворачивание (скрыто сразу после pause, не дожидаясь stop)
- [x] 1.2 Реализовать чистую модель `EventNotificationModelFactory`: заголовки, тексты, публичные версии для экрана блокировки, набор действий для запроса подключения, входящих файлов (с папкой и без), текста, ссылки и итогов (все успешны, часть не удалась); переиспользовать `safeNotificationName()`. Проверка: JVM-тесты на каждый вид, русские формы множественного числа, отсутствие pairing code и token, безопасные имена с путями, управляющими и bidi-символами
- [x] 1.3 Реализовать `AndroidEventNotificationPublisher`: канал `devicebridge_events` «События» с `IMPORTANCE_HIGH`, `VISIBILITY_PRIVATE` с `setPublicVersion`, id по правилам design.md, `setTimeoutAfter` для запроса, группа для текста, отмена по id и массовая отмена при остановке сервера. Проверка: source-контракт (отдельный файл, высокий приоритет, `FLAG_IMMUTABLE`, нет содержимого в extras) и instrumented-тест на эмуляторе API 37: канал создан с высокой важностью, публичная версия без содержимого

## 2. Android: события

- [x] 2.1 Реализовать `EventNotificationCoordinator` на app scope: разница снимков запросов подключения, файлов и текста; первый снимок после старта сервера служит базой; при видимом приложении обновлять базу без публикации; снимать уведомления о запросе и о файлах при разрешении события; очищать при закрытии поколения сервера. Проверка: JVM-тесты с поддельными `StateFlow` на каждое правило, включая повтор файла после сбоя и истечение запроса
- [x] 2.2 Группировка файлов: одно уведомление о входящих файлах на `ownerSessionId` с числом и общим размером; итоговое уведомление, когда все файлы группы в одном направлении терминальны; файлы, принятые автоприёмом, не дают уведомления о входящих, но дают итог. Проверка: JVM-тесты на пачку из трёх файлов, частичный сбой и автоприём доверенного браузера
- [x] 2.3 Подключить координатор в DI и запускать его в `DeviceBridgeApplication`; начало и конец поколения сервера координатор видит по `BrowserSessionState.generationId`, отдельный хук в runtime не нужен. Проверка: `EventNotificationCoordinatorTest`: после остановки сервера уведомления о тексте, запросах и входящих файлах сняты, а итоговые остаются

## 3. Android: действия

- [x] 3.1 Добавить неэкспортируемый `EventNotificationActionReceiver` с `goAsync()` и действиями «Копировать», «Принять», «Отклонить»; уточнить `ServerManifestContractTest` и `ServerManifestInstrumentedTest`: разрешён только этот receiver, `exported=false`, без системных intent-filter. Проверка: контрактные тесты манифеста
- [x] 3.2 «Копировать»: взять текст по `TextMessageId` из `TextTransferRepository.state` и записать в буфер обмена; если сообщения уже нет - только снять уведомление. Проверка: JVM-тест обработчика с поддельным буфером обмена
- [x] 3.3 «Принять»: `NotificationAcceptedTransfers` и третье основание приёма в `TrustedAutoAcceptController`; файлы из `QUEUED` принимаются по мере продвижения; недоступная папка переводит уведомление в «Откройте приложение, чтобы выбрать папку». Проверка: JVM-тесты в `TrustedAutoAcceptControllerTest` на пачку из трёх файлов, отсутствие папки и уже принятый вручную файл
- [x] 3.4 «Отклонить»: `cancel` для каждого ещё не начатого файла группы. Проверка: JVM-тест, что начатый файл не отменяется, а браузер получает отмену остальных
- [x] 3.5 Открытие экранов: `EXTRA_OPEN_SECTION` (`home`, `text`, `files`) в `MainActivity` (`onCreate` и `onNewIntent`, удаление extra после использования), навигация в `DeviceBridgeApp`; «Открыть» для ссылки - `getActivity` с `ACTION_VIEW` только для ссылки, прошедшей `TextContentClassifier`. Проверка: instrumented-тест, что intent с каждым значением открывает нужный экран, а повторное создание Activity не повторяет навигацию

## 4. Android: настройки

- [x] 4.1 Строка «Уведомления о событиях» в разделе «Сервер»: «Включены» или «Выключены в системе» по разрешению и состоянию канала (перечитывается при возврате на экран); нажатие открывает системные настройки канала или приложения. Строка самостоятельна, как «Плитка в быстрых настройках», без `SettingsViewModel`. Проверка: JVM-тест правила «приложение и канал разрешены» и Compose-тест строки

## 5. Web

- [x] 5.1 Реализовать `browserNotificationPreferenceStore.ts` (ключ `devicebridge.notifications.v1`, `{version, enabled, hideContent}`) по образцу хранилища предупреждения. Проверка: vitest с `memoryStorage` и `throwingStorage`: значение по умолчанию, сохранение, неверная запись удаляется
- [x] 5.2 Реализовать `eventNotificationController.ts` с портами `NotificationPort`, `VisibilityPort`, `TitlePort`: доступность только по HTTPS, разрешение только по нажатию, показ только при скрытой вкладке или окне не в фокусе, база из первого snapshot, увиденные id (до 500), объединение по `tag` с «и ещё N», потеря связи только при переходе в `needsUserAction`, `sessionLost` или `offline`, скрытие содержимого, обрезка текста до 120 символов и имён до 64. Проверка: vitest с поддельными портами на каждое правило, включая переподключение без повторов и HTML в тексте
- [x] 5.3 Счётчик в заголовке вкладки: `(N) <исходный заголовок>` при скрытой вкладке, сброс при возврате, работа по HTTP и без разрешения. Проверка: vitest
- [x] 5.4 Панель «Уведомления» в `index.html` и её view: состояния `unavailable`, `default`, `enabled` (с переключателями «Скрывать содержимое» и «Выключить»), `denied`; доступность с клавиатуры и объявления через `preference-announcer`. Проверка: vitest в jsdom на каждое состояние и `accessibilityStructure`
- [x] 5.5 Подключить контроллер в `main.ts`: обернуть `render` callbacks и callbacks сокета (`text.received`, `file.offer`, snapshot, переходы сессии), клик по уведомлению фокусирует вкладку и карточку; обновить `mainFileWiring.contract.test.ts` (контроллер не знает о `SessionController`, `TextTransferController`, `FileTransferController`). Проверка: контрактный тест и vitest обвязки
- [x] 5.6 Значок уведомления из `link[rel="icon"]`. Проверка: privacy-контракт по-прежнему видит ровно одно упоминание `serviceWorker` и ни одного литерала `http(s)://`, release policy проходит
- [x] 5.7 Обновить визуальные снимки Playwright с новой панелью и добавить compatibility-проверку: страница по HTTPS (файлы dev-сервера под https-origin через `page.route`) с `grantPermissions(["notifications"])`, поддельный сокет присылает текст при скрытой вкладке, создаётся ровно одно уведомление, service worker не регистрируется. Проверка: `test:visual` и `test:compat` для Chrome и Edge

## 6. Проверка

- [x] 6.1 Прогнать JVM-тесты, lint, web-тесты, visual, compatibility и release policy. Проверка: все проходят
- [x] 6.2 Сквозная проверка на эмуляторе: приложение свёрнуто, браузер отправляет запрос подключения, файлы и текст; уведомления появляются, «Принять», «Отклонить» и «Копировать» работают, на экране блокировки нет содержимого, после остановки сервера уведомления о тексте сняты. Проверка: запись результатов в `docs/verification/`
- [x] 6.3 Ручная проверка на Windows 11 в Chrome и Edge по HTTPS: уведомление в свёрнутом окне, клик возвращает на карточку, «Скрывать содержимое», отказ в разрешении, SVG-значок отображается (иначе PNG по design.md), часовой простой скрытой вкладки. Проверка: запись результатов в `docs/verification/`
- [x] 6.4 Обновить `docs/user-guide.md`: как включить уведомления на компьютере, почему только в защищённом режиме, что вкладка должна быть открыта, как заглушить события на телефоне. Проверка: release policy проходит
