## 1. Протокол и хранилище незавершённых частей

- [x] 1.1 Добавить в Kotlin и TS совместимые сообщения `file.upload_offset.request` и `file.upload_offset` (`offsetBytes`), сохранив `FILE_PROTOCOL_VERSION = 1`; проверить validator/parser-тесты обеих сторон, включая отказ для чужой версии и отрицательного offset.
- [x] 1.2 Добавить Room-таблицу `partial_uploads` (sha256, size, name, treeUri, partialDocumentUri, mime, bytes, updatedAt) с миграцией 1 → 2 и экспортом схемы; проверить migration-тест и то, что правила backup по-прежнему исключают БД.
- [x] 1.3 Реализовать репозиторий незавершённых частей: поиск по ключу, upsert, удаление, cleanup записей старше 24 ч и без существующего partial; проверить JVM-тестами с фиктивными часами и фиктивным provider.

## 2. Android: сохранение и продолжение upload

- [x] 2.1 Разделить завершение partial на `abort()` и `retain()`: network failure, потеря session и остановка сервера для файлов ≥ 8 МиБ сохраняют partial и запись, а отмена, checksum mismatch и мелкие файлы удаляют; проверить JVM-тестами всех веток.
- [x] 2.2 Открывать partial в режиме `"rw"` с `truncate`/`position(offset)` и fallback на запись с нуля, если provider не даёт seekable descriptor; проверить тестом с фиктивным provider без seek.
- [x] 2.3 Восстанавливать SHA-256 по фактическому префиксу partial перед приёмом и использовать прочитанную длину как offset; проверить тестом с усечённым и повреждённым префиксом, где итоговое несовпадение даёт checksum mismatch и удаление части.
- [x] 2.4 Сопоставлять подтверждённый offer с записью по SHA-256, размеру, имени и папке, включая автоприём; при другой папке начинать с нуля; проверить JVM-тестами coordinator.
- [x] 2.5 Изменить retry: item с сохранённой частью помечается как продолжение, автоприём подтверждает его в папку по умолчанию, если часть лежит в ней, и повторный ручной approve в том же generation не нужен; после подготовки progress поднимается до offset; проверить тестами coordinator и контроллера автоприёма.
- [x] 2.6 Добавить `POST /api/v1/files/{id}/upload-offset`, принимать `POST /api/v1/files/{id}` с заголовком `X-DeviceBridge-Upload-Offset`, отклонять несовпадающий с подготовленным offset кодом `409` без записи и проверять `Content-Length = size − offset`; проверить route-тестами.
- [x] 2.7 Учитывать при проверке свободного места только оставшиеся байты: предварительной проверки для B→A нет, нехватка места обнаруживается при записи оставшихся байтов и не удаляет сохранённую часть; проверить тестом.
- [x] 2.8 Запускать cleanup при старте приложения и сервера; проверить тестом, что просроченная часть удаляется и следующий offer начинается с нуля.

## 3. Android: продолжение download

- [x] 3.1 Отдавать `Accept-Ranges: bytes` и `ETag: "sha256-<hex>"`, обрабатывать одиночный `Range` ответом `206` и `Content-Range`, `If-Range`/ETag mismatch ответом `412`, прочие диапазоны ответом `416`; проверить route-тестами.
- [x] 3.2 Разрешить повторное использование grant для ranged-продолжения того же item в течение 15 минут после обрыва в том же generation, переводить item FAILED → TRANSFERRING с progress от offset и отвечать `409`, если направление занято; проверить тестами grant registry и coordinator.

## 4. Web

- [x] 4.1 Отправлять `file.slice(offset)` с заголовком offset и корректным progress от offset; проверить Vitest-тестом uploader.
- [x] 4.2 Показывать «Продолжить» с уже переданным объёмом для failed item с `resumableBytes`, этап «Проверка сохранённой части» и продолжение с N МБ для совпавшего offer; проверить Vitest-тестами view и controller.
- [x] 4.3 Для прерванного download показывать подсказку о «Возобновить» в менеджере загрузок браузера и повтор с начала; проверить Vitest-тестом и обновить visual-снимки, если меняется карточка.

## 5. Android UI

- [x] 5.1 На экране файлов и Home показывать «Продолжить» и «Продолжение с N МБ» вместо «Повторить», если сохранена часть; проверить Compose-тестами.
- [x] 5.2 В Settings (карточка «Передача файлов») показать число и размер незавершённых файлов с действием «Удалить незавершённые»; проверить Compose-тестом и тестом ViewModel.

## 6. Документация

- [x] 6.1 Обновить `docs/user-guide.md` (продолжение, 24 ч, удаление, возобновление download) и ТЗ; выполнить `openspec validate add-resumable-file-transfer --strict`.

## 7. Проверка

- [x] 7.1 Прогнать `:app:testDebugUnitTest`, `:app:lintDebug`, `:app:verifyReleasePolicy`, полный набор web gates и `connectedDebugAndroidTest` на API 29 и API 37.1; проверить отсутствие регрессий.
- [x] 7.2 E2E на release-сборке, API 29 и 37.1: оборвать upload 500 МиБ на середине отключением Wi-Fi эмулятора, перезапустить сервер, переподключиться, отправить тот же файл и убедиться, что передача продолжилась с сохранённого offset, а SHA-256 на телефоне совпал; повторить «Продолжить» в той же вкладке при обрыве WebSocket.
- [x] 7.3 E2E download: оборвать ranged-загрузку 500 МиБ в Chrome, возобновить её штатно и сравнить SHA-256 скачанного файла; проверить отказ после 15 минут и после перезапуска сервера.
