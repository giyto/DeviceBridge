## 1. Настройка автоприёма

- [ ] 1.1 Добавить `autoAcceptTrustedFiles` в `DeviceSettings` (по умолчанию `false`) и ключ DataStore; проверить JVM-тестами чтение значения по умолчанию, сохранение и сохранность после пересоздания repository.
- [ ] 1.2 Добавить в `SettingsViewModel` включение/выключение с отказом без доступной destination и вычисляемое состояние «приостановлен» при недоступной папке; проверить unit-тестами все три состояния.
- [ ] 1.3 Добавить переключатель с пояснением в Settings UI под «Папка для входящих файлов»; проверить Compose-тестом доступность, disabled-состояние без папки и текст «приостановлен».

## 2. Автоприём на уровне сервиса

- [ ] 2.1 Написать failing JVM-тесты `TrustedAutoAcceptController`: trusted session + включено + папка доступна → `approve`; обычная session, выключенная настройка, отсутствующая папка → нет `approve`; повторная обработка и `InvalidState` от ручного подтверждения не ломают состояние; проверить, что тесты падают до реализации.
- [ ] 2.2 Реализовать `TrustedAutoAcceptController` (открытие persisted destination, регистрация lease, `approve`, идемпотентность по transferId, публикация множества автопринятых и причины приостановки); проверить, что тесты 2.1 проходят.
- [ ] 2.3 Подключить контроллер к жизненному циклу server runtime (старт с generation, остановка и очистка при stop/смене сети); проверить JVM-тестом, что после stop нет обработки и множество очищено.
- [ ] 2.4 Проверить отзыв доверия: offers отозванного trusted browser, ещё не начатые, не принимаются автоматически; покрыть unit-тестом на фейковом session state.

## 3. Отображение автоприёма на Android

- [ ] 3.1 Показать в `FileScreen` и «Активных передачах» главного экрана метку «Принят автоматически» и имя браузера; проверить Compose-тестом наличие метки и доступного описания.
- [ ] 3.2 Показать причину «Автоприём приостановлен: папка недоступна» у item, ожидающего ручного решения; проверить Compose-тестом, что ручные действия принятия остаются доступны.

## 4. Вставка изображения из буфера в web

- [ ] 4.1 Написать failing Vitest-тесты обработчика `paste`: изображение вне поля → в draft с именем «Скриншот …» и верным расширением; текст в поле → штатная вставка; изображение без текста в поле → draft + сообщение; пустой буфер → без изменений; без session → игнор; проверить падение до реализации.
- [ ] 4.2 Реализовать обработчик `paste` в file transfer view с передачей файлов в `addFiles`, генерацией имени и сообщением через announcer; проверить, что тесты 4.1 проходят и `npm run typecheck` чист.
- [ ] 4.3 Добавить подсказку про `Ctrl+V` в зону файлов с учётом светлой/тёмной темы и ширин 360/768/1920; проверить обновлёнными visual snapshots.
- [ ] 4.4 Добавить Playwright-сценарий в Chrome и Edge: вставка изображения через синтетическое событие `paste` попадает в draft и уходит на сервер только после подтверждения; проверить `npm run test:compatibility`.

## 5. Интеграция и проверка

- [ ] 5.1 Прогнать `:app:testDebugUnitTest`, `:app:lintDebug`, `:app:verifyReleasePolicy` и полный набор web gates; проверить отсутствие регрессий.
- [ ] 5.2 Выполнить `connectedDebugAndroidTest` на API 29 и API 37.1; проверить отсутствие падений.
- [ ] 5.3 На release-сборке и эмуляторах API 29 и 37.1 проверить: 10 файлов от запомненного браузера принимаются без касаний при свёрнутом приложении; одноразовый браузер требует подтверждения; отзыв доверия и отозванная папка переводят offer в ручной режим без потери и ложного «Завершено»; SHA-256 файлов совпадает.
- [ ] 5.4 В Chrome и Edge на Windows 11 проверить вставку реального скриншота (`Win+Shift+S`, затем `Ctrl+V`) и получение файла на телефоне с совпадающим SHA-256.
- [ ] 5.5 Обновить `docs/user-guide.md` и ТЗ (раздел файлов и настроек) описанием автоприёма и вставки; выполнить `openspec validate add-trusted-auto-accept-and-clipboard-paste --strict`.
