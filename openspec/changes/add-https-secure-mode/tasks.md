## 1. Спайк и зависимости

- [ ] 1.1 Добавить `bcpkix-jdk18on`/`bcprov-jdk18on` в `gradle/libs.versions.toml` и `app/build.gradle.kts`, собрать `assembleRelease` и записать прирост размера APK после R8 в design.md (Open Questions). Проверка: сборка проходит, прирост не больше ~1,5 МБ или отмечен в design.md
- [ ] 1.2 Спайк на эмуляторе API 29 и API 37: TLS на платформенном `SSLServerSocket` с чтением первого байта и `SSLSocketFactory.createSocket(socket, consumed, true)`. Если Conscrypt этого не поддерживает, зафиксировать в design.md переход на `SSLEngine`. Проверка: instrumented-тест принимает и TLS, и обычный HTTP на одном порту на обоих API
- [ ] 1.3 Спайк подписи: ключ EC P-256 в AndroidKeyStore подписывает сертификат через BC `ContentSigner`, а Conscrypt использует ключ сертификата сервера из AndroidKeyStore в `KeyManager`. Проверка: instrumented-тест устанавливает TLS-соединение клиентом, доверяющим только этому корню

## 2. Сертификаты

- [ ] 2.1 Ввести `TlsKeyStore` с реализацией на AndroidKeyStore и программной реализацией для JVM-тестов. Проверка: JVM-тест создаёт, читает и удаляет ключи программной реализации
- [ ] 2.2 Реализовать выпуск корня (EC P-256, `CA:true pathLen 0`, `keyCertSign|cRLSign`, critical name constraints для 10/8, 172.16/12, 192.168/16 и `devicebridge.local`, срок 10 лет). Проверка: JVM-тест разбирает расширения и сверяет каждое поле
- [ ] 2.3 Реализовать выпуск сертификата сервера (SAN IP и `devicebridge.local`, `serverAuth`, `notBefore` −24 ч, срок 30 дней). Проверка: JVM PKIX `CertPathValidator` принимает цепочку для частного IP и отклоняет сертификат для публичного IP и публичного имени, подписанный тем же корнем
- [ ] 2.4 Реализовать хранилище материалов в `noBackupFilesDir/tls/` с правилом перевыпуска (адреса нет в SAN или осталось меньше 7 дней), исключить каталог из backup. Проверка: JVM-тесты на смену IP, приближение срока и повреждённый файл (ошибка, а не тихий HTTP)
- [ ] 2.5 Реализовать сброс: удаление ключей и файлов и выпуск нового корня. Проверка: JVM-тест подтверждает, что после сброса старый корень не проверяет новый сертификат сервера
- [ ] 2.6 Реализовать отпечатки SHA-1 (группы по 8 заглавных hex, как в окне Windows) и SHA-256. Проверка: JVM-тест на эталонном сертификате с заранее посчитанными значениями

## 3. TLS-приёмник и реестр соединений

- [ ] 3.1 Реализовать реестр соединений `локальный порт → (IPv4, secure)` с удалением при закрытии. Проверка: JVM-тест конкурентной регистрации и удаления
- [ ] 3.2 Реализовать приёмник: определение протокола по первому байту, TLS 1.2+, перекачка к CIO на `127.0.0.1` с буфером 64 КиБ, регистрация в реестре. Проверка: JVM-тест с реальным `SSLSocket`-клиентом и обычным HTTP-клиентом на один порт
- [ ] 3.3 Добавить лимит в 64 соединения, таймаут рукопожатия и первого байта 10 с со снятием после рукопожатия и закрытие всех соединений при остановке. Проверка: JVM-тесты на зависшее рукопожатие (закрыто, другие живы), долгую передачу дольше таймаута (не рвётся) и освобождение порта после stop
- [ ] 3.4 Проверить отказ по TLS 1.1 и ниже. Проверка: JVM-тест клиента с `TLSv1.1` получает ошибку рукопожатия

## 4. Интеграция с сервером

- [ ] 4.1 Добавить `secureModeEnabled` в `DeviceSettings`, хранилище настроек и use case обновления (по умолчанию `false`). Проверка: JVM-тесты репозитория настроек на значение по умолчанию и сохранение
- [ ] 4.2 Добавить в `ServerEndpoint` признак `secure` и схему в `url`, обновить главный экран, уведомление и плитку. Проверка: JVM-тесты `ServerEndpoint` и существующие тесты уведомления и плитки с `https://`
- [ ] 4.3 В `KtorServerRuntimeFactory` в защищённом режиме: подготовить сертификаты, привязать CIO к `127.0.0.1:0`, запустить приёмник на публикуемом порту, брать `sourceIpv4` из реестра и отвергать незарегистрированные соединения. Проверка: JVM-тест runtime, в котором pairing по TLS видит IPv4 клиента, а прямое подключение к loopback получает 403
- [ ] 4.4 Передать ожидаемую схему в `SessionRequestSecurityPolicy` и `WebRoutes.isAllowedWebRequest`. Проверка: JVM-тесты на `Origin: https://…` (принят в защищённом режиме, отклонён в HTTP) и наоборот
- [ ] 4.5 Добавить перехватчик для незащищённых соединений в защищённом режиме: разрешены `/`, ассеты установки и `/devicebridge-ca.crt`, API, WebSocket, text и file маршруты получают 403, а `tls-probe` (204) доступен только по TLS. Проверка: JVM-тесты маршрутов в обоих вариантах соединения
- [ ] 4.6 Проверить pairing, текст, загрузку и скачивание файлов, продолжение загрузки через upload-offset и скачивание с `Range` по TLS. Проверка: JVM-тесты runtime по HTTPS для этих сценариев
- [ ] 4.7 Перезапускать сервер при переключении режима и сбросе, если он работает, без отзыва запомненных браузеров. Проверка: JVM-тест контроллера lifecycle (одна остановка и один запуск, trusted-записи не изменились)

## 5. Android UI

- [ ] 5.1 Добавить в `SettingsScreen` карточку «Защищённый режим (HTTPS)»: переключатель как в `AutoAcceptRow`, пояснение о повторном входе браузера, диалог подтверждения при работающем сервере. Проверка: Compose-тест переключателя и диалога
- [ ] 5.2 Показать отпечатки SHA-1 и SHA-256 (под «Подробнее»), кнопку «Сбросить сертификат» с подтверждением и инструкцией удалить старый корень с компьютера. Проверка: Compose-тест отображения отпечатка и потока сброса
- [ ] 5.3 Показывать recoverable error запуска при сбое сертификатов с действием «Сбросить сертификат». Проверка: Compose-тест состояния ошибки на главном экране

## 6. Web

- [ ] 6.1 Добавить вторую точку входа Vite `setup.html`: скачивание корня, вкладки инструкций Windows/macOS/Chrome/Edge/Firefox с выбором по `userAgent`, просьба сверить отпечаток с телефоном, проверка доверия через `tls-probe` и `location.replace` на HTTPS, кнопка повторной проверки. Проверка: web unit-тесты выбора ОС и логики проверки, `npm run -s typecheck|test|build`
- [ ] 6.2 Отдавать страницу установки с CSP, где `connect-src` дополнительно разрешает точный `https://IP:порт`. Проверка: JVM-тест заголовков ответа страницы установки
- [ ] 6.3 В `securityWarningController` на HTTPS показывать пометку «Соединение зашифровано» вместо предупреждения о HTTP. Проверка: web unit-тесты для обеих схем и `npm run -s test:visual`
- [ ] 6.4 Обновить release policy и проверки совместимости под вторую точку входа. Проверка: `npm run -s verify:release-policy` и `npm run -s test:compatibility`

## 7. Сквозная проверка

- [ ] 7.1 E2E на эмуляторе: Chrome с `--ignore-certificate-errors-spki-list=<SPKI корня>` на отдельном профиле открывает `https://`, проходит pairing и передаёт текст и файл в обе стороны. Отдельно проверить, что без флага открывается страница установки по `http://`. Хранилище сертификатов хоста не менять
- [ ] 7.2 Ручная проверка на компьютере пользователя после установки корня по инструкции (флаг из 7.1 подавляет ошибки всей цепочки и для этого не подходит): Chrome и Edge открывают `https://IP:порт` без предупреждения, а тестовый сервер с сертификатом на публичное имя, подписанным тем же корнем, отклоняется. После проверки удалить корень. Результат (соблюдают ли браузеры name constraints) записать в design.md
- [ ] 7.3 Замер на реальном телефоне: загрузка и скачивание файла 1 ГиБ по HTTPS против HTTP, цель не ниже 70 %. Результат записать в design.md
- [ ] 7.4 Прогнать все JVM-, web- и затронутые Compose-тесты и `openspec validate add-https-secure-mode --strict`. Проверка: всё зелёное
